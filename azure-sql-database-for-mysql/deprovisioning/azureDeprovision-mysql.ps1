# The solution is deleted by using the following process:
# You can clean up the resources that you created in two ways. You can delete the Azure resource group, which includes all the resources in the resource group. If you want to keep the other resources intact, delete only the one server resource.
# To delete the entire resource group including the newly created server, use the az group delete azure cli command:
az group delete --name msp-data-platform-azure-mysql-teff-woodside -y



